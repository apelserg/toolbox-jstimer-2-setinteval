"use strict";

//===
// Объект для сохранения результатов
//===
APELSERG.MODEL.Values = function (type, tick, prev, curr) {
    this.Type = type;
    this.Tick = tick;
    this.Prev = prev;
    this.Curr = curr;
}

